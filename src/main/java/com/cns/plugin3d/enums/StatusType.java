package com.cns.plugin3d.enums;

public enum StatusType {
    ACTIVE,
    INACTIVE,
    DRAFT,RELEASED,
    ONLINE, OFFLINE, UPDATING, ERROR, DELETED, //device_status_history
    PENDING, IN_PROGRESS, SUCCESS, FAILED //campaign_device_relations
}
